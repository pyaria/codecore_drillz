<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_path %>" ><i class='glyphicon glyphicon-home'></i> Home</a>
      <%= link_to "Drill Groups", drill_groups_path, class:"nav navbar-brand" %>
      <%= link_to "My Drill Groups", user_drill_groups_path(current_user), class:"nav navbar-brand" %>
    </div>

<!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Hello <%= current_user.first_name.capitalize %> <%= current_user.last_name.capitalize %>! <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li> <%= link_to("Log Out", destroy_user_session_path, method: :delete) %></li>
            </ul>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Log In | Sign Up<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li> <%= link_to "Sign Up", new_user_registration_path %></li>
              <li> <%= link_to "Log In", new_user_session_path %></li>
        <% end %>
      </ul>


    </div>
  </div>
</nav>
